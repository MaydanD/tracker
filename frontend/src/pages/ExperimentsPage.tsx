import { PlaceholderPage } from '../components/PlaceholderPage'

export function ExperimentsPage() {
  return (
    <PlaceholderPage
      title="Experiments"
      plannedIn="Stage 10"
      summary="Define a temporary self-experiment and compare the period before, during and after it."
    />
  )
}
