"""personal experiments

Stage 10 stores only the user's own definition of an experiment: a title, the
hypothesis and protocol text, the planned window and an optional cancellation
date. Status is derived from the dates plus ``cancelled_on`` at read time,
so the lifecycle can never disagree with the calendar. No comparison values are
stored — every metric is recomputed from the canonical Stage 4/7 dataset.

Revision ID: c3f1a7b24d90
Revises: b2631e796164
Create Date: 2026-09-26 10:00:00.000000
"""

from __future__ import annotations

from collections.abc import Sequence

import sqlalchemy as sa
from alembic import op


revision: str = 'c3f1a7b24d90'
down_revision: str | None = 'b2631e796164'
branch_labels: str | Sequence[str] | None = None
depends_on: str | Sequence[str] | None = None


def upgrade() -> None:
    # ### commands auto generated by Alembic - please adjust! ###
    op.create_table('experiments',
    sa.Column('id', sa.Integer(), nullable=False),
    sa.Column('title', sa.String(length=120), nullable=False),
    sa.Column('hypothesis', sa.Text(), nullable=False),
    sa.Column('protocol', sa.Text(), nullable=False),
    sa.Column('start_date', sa.Date(), nullable=False),
    sa.Column('end_date', sa.Date(), nullable=False),
    sa.Column('cancelled_on', sa.Date(), nullable=True),
    sa.Column('created_at', sa.DateTime(timezone=True), nullable=False),
    sa.Column('updated_at', sa.DateTime(timezone=True), nullable=False),
    sa.CheckConstraint('length(trim(title)) > 0', name=op.f('ck_experiments_title_nonempty')),
    sa.CheckConstraint('start_date <= end_date', name=op.f('ck_experiments_date_order')),
    sa.PrimaryKeyConstraint('id', name=op.f('pk_experiments'))
    )
    with op.batch_alter_table('experiments', schema=None) as batch_op:
        batch_op.create_index(batch_op.f('ix_experiments_start_date'), ['start_date'], unique=False)

    # ### end Alembic commands ###


def downgrade() -> None:
    # ### commands auto generated by Alembic - please adjust! ###
    with op.batch_alter_table('experiments', schema=None) as batch_op:
        batch_op.drop_index(batch_op.f('ix_experiments_start_date'))

    op.drop_table('experiments')
    # ### end Alembic commands ###
